[package]
name = "benches"
publish.workspace = true
edition.workspace = true

[dependencies]
metalogue = { path = "..", default-features = false }
criterion.workspace = true

[[bench]]
name = "adder_kernel_compilation"
path = "adder_kernel_compilation.rs"
harness = false
