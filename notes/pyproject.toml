[project]
name = "notes"
version = "0.0.1"
description = "Notes on Parallel Programming for Apple Silicon"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mlx>=0.30.3",
    "quarto>=0.1.0",
]
