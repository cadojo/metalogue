[package]
name = "tests"
publish.workspace = true
edition.workspace = true

[dev-dependencies]
metalogue = { path = "..", default-features = false }

[[test]]
name = "gpu_compute"
path = "gpu_compute.rs"
