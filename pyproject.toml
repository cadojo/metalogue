[build-system]
requires = ["maturin>=1.10,<2.0"]
build-backend = "maturin"

[project]
name = "metalogue"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]
dynamic = ["version"]

[tool.maturin]
manifest-path = "crates/metalogue/Cargo.toml"
python-source = "crates/metalogue/pyo3"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ty>=0.0.12",
]
